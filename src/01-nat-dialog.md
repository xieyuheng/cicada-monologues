---
title: 自然数
---

- [dialog]

  - 什么是数呢？

  - 数就是数数。

    一、二、三、……

    「数」这个概念，可能就是我们在生活中所识别出来的一个模式吧。

  - 没错，我们应该从「零」开始，然后数下一个，再下一个……

    下面的定义可以描述这种模式吗？

    ``` cicada
    datatype Nat {
      zero: Nat
      add1(prev: Nat): Nat
    }
    ```

  - 这是代码吗？

    `zero` 是「零」，`add1` 是「加一」，
    但是 `datatype`、`Nat` 还有 `prev` 是什么意思呢？
